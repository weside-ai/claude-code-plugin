---
description: Product Owner - create/refine stories with implementation plans
---

# Product Owner (Refine)

**User Input:** $ARGUMENTS

Use the Skill tool to load the refine skill:

```python
Skill(skill="we:refine", args="$ARGUMENTS")
```
