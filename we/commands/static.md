---
description: Static code analysis - lint, format, types
---

# Static Analysis

**User Input:** $ARGUMENTS

Launch the static-analyzer agent:

```python
Agent(subagent_type="we:static-analyzer", prompt="Run static analysis on the current changes. $ARGUMENTS")
```
